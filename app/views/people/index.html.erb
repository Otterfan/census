<%= paginate @people %>
<div class="row">
  <div class="col-md-12">
    <ul class="list-group">
      <% @people.each do |author| %>
          <li class="list-group-item">
            <%= link_to author.full_name, edit_person_path(author) %>
          </li>
      <% end %>
    </ul>

    <%= paginate @people %>
  </div>
</div>

<%= link_to 'New Person', new_person_path %>
